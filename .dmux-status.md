# Phase 3 Stack Upgrade — Status

**Last updated:** 2026-04-26 (start of session)
**Branch:** phase3-stack-upgrade

## Steps

- [x] Step 1: Bun — `bun.lock` already exists, CI already on bun, no package-lock.json. Running `bun install` to hydrate node_modules.
- [ ] Step 2: viem — add as explicit dep (matching thirdweb's bundled version)
- [ ] Step 3: Remove ethers.js — 6 files need changes
- [ ] Step 4: TypeScript migration
- [ ] Step 5: GraphQL codegen

## Ethers usages to fix (Step 3)

- `src/features/usage/ManagePlanModal.jsx` — `ethers.parseUnits` → viem `parseUnits`
- `src/hooks/useRecentActivity.js` — `ethers.formatEther` → viem `formatEther`
- `src/hooks/useUsagePlan.js` — `ethers.formatEther` (3x) → viem `formatEther`
- `src/hooks/useLiveResponse.js` — `new ethers.Interface` (2x) → viem `getAbiItem` + `formatAbiItem`
- `src/hooks/useChatMutations.jsx` — `ethers.toUtf8Bytes`, `ethers.hexlify`, `ethers.Interface`, `parseLog`, `getEvent`, `getError`
- `src/lib/syncService.js` — `ethers.toUtf8String` → viem `hexToBytes` + native TextDecoder

## Status: running bun install
